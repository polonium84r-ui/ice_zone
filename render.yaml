services:
  - type: web
    name: ice-zone-api
    runtime: node
    rootDir: server
    buildCommand: npm install
    startCommand: node seed.js && node server.js
    envVars:
      - key: PORT
        value: 4000
      - key: THIRST_JWT_SECRET
        generateValue: true
